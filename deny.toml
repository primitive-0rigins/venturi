[advisories]
# `paste` is transitive through nalgebra → simba. RustSec reports it as
# unmaintained, not vulnerable; no safe upstream replacement is available.
ignore = ["RUSTSEC-2024-0436"]

[licenses]
confidence-threshold = 0.8
allow = [
  "Apache-2.0",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "MIT",
  "Unicode-3.0",
  "Zlib",
]
